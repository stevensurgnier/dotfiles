## help setting options
## > help(options)

## set prompt
options(prompt="> ")

## set default cran mirror
## > chooseCRANmirror()
## > options("repos")
## copy url and paste into `options(repos="url")`
options(repos="http://ftp.osuosl.org/pub/cran")

## set default number of digits to print when printing numeric values
options(digits=6)
